# AL TAMIMI - Система управления колледжем

## Описание проекта

Полнофункциональная система управления колледжем AL TAMIMI с поддержкой:
- 🎓 Управления студентами и группами
- 📅 Расписание занятий
- 📝 Журнала посещаемости
- 📊 Системы оценок и модулей
- 📈 Аналитики успеваемости студентов
- 📢 Новостей и объявлений
- 🔔 Системы уведомлений
- 👥 Ролевой системы доступа
- 📱 Адаптивного интерфейса

## Архитектура проекта

```
al-tamimi-system/
├── backend/                    # Node.js/Express сервер
│   ├── models/                # MongoDB модели
│   ├── controllers/           # Бизнес-логика
│   ├── routes/               # API маршруты
│   ├── middleware/           # Промежуточное ПО
│   ├── server.js             # Точка входа
│   └── package.json          # Зависимости
│
├── frontend/                  # React приложение
│   ├── src/
│   │   ├── pages/           # Страницы
│   │   ├── components/      # Компоненты
│   │   ├── services/        # API сервисы
│   │   ├── context/         # Context API
│   │   ├── styles/          # CSS файлы
│   │   ├── App.js           # Главный компонент
│   │   └── index.js         # Точка входа
│   └── package.json         # Зависимости
│
└── database/                 # Схемы БД
```

## Требования

- Node.js 14+
- MongoDB 4.0+
- npm или yarn

## Установка и запуск

### Backend

```bash
cd backend
npm install

# Создать .env файл
cp .env.example .env

# Заполнить переменные окружения
# MONGODB_URI=mongodb://localhost:27017/al-tamimi
# JWT_SECRET=your_secret_key
# PORT=5000

# Запуск сервера
npm start

# или для разработки с автоперезагрузкой
npm run dev
```

### Frontend

```bash
cd frontend
npm install

# Создать .env файл
echo "REACT_APP_API_URL=http://localhost:5000/api" > .env

# Запуск приложения
npm start
```

## Роли и права доступа

### 🔐 Роли в системе:

1. **Администратор (Admin)**
   - Управление всеми пользователями
   - Создание групп и расписания
   - Управление модулями
   - Статистика колледжа

2. **Ректор (Rector)**
   - Создание админов
   - Управление деканами
   - Глобальная статистика

3. **Президент колледжа (President)**
   - Высший уровень доступа
   - Стратегическое управление

4. **Декан (Dean)**
   - Управление факультетом
   - Одобрение оценок
   - Управление расписанием

5. **Учитель (Teacher)**
   - Отметить посещаемость
   - Внести оценки
   - Управление своими группами

6. **Студент (Student)**
   - Просмотр расписания
   - Просмотр своих оценок
   - Просмотр статистики
   - Просмотр новостей

7. **Староста (Group Leader)**
   - Помощь в управлении группой
   - Просмотр информации группы

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Получить профиль

### Студенты
- `GET /api/students/group/:groupId` - Студенты группы
- `GET /api/students/:id` - Информация студента
- `GET /api/students/:id/stats` - Статистика студента
- `POST /api/students` - Создать студента

### Расписание
- `GET /api/schedule/group/:groupId` - Расписание группы
- `GET /api/schedule/group/:groupId/day/:dayOfWeek` - Расписание на день
- `POST /api/schedule` - Создать расписание
- `PUT /api/schedule/:id` - Обновить расписание
- `DELETE /api/schedule/:id` - Удалить расписание

### Посещаемость
- `POST /api/attendance` - Отметить посещаемость
- `GET /api/attendance/student/:studentId` - История посещаемости
- `GET /api/attendance/student/:studentId/stats` - Статистика посещаемости
- `GET /api/attendance/group/:groupId/date/:date` - Посещаемость группы

### Оценки
- `POST /api/grades` - Создать оценку
- `GET /api/grades/student/:studentId` - Оценки студента
- `GET /api/grades/subject/:subject` - Оценки по предмету
- `PUT /api/grades/:id` - Обновить оценку

### Группы
- `GET /api/groups` - Все группы
- `GET /api/groups/:id` - Информация группы
- `POST /api/groups` - Создать группу
- `PUT /api/groups/:id` - Обновить группу
- `POST /api/groups/:groupId/students/:studentId` - Добавить студента

### Аналитика
- `GET /api/analytics/student/:studentId` - Аналитика студента
- `GET /api/analytics/group/:groupId` - Аналитика группы

### Новости и объявления
- `GET /api/news` - Все новости
- `POST /api/news` - Создать новость
- `GET /api/news/:id` - Новость по ID
- `PUT /api/news/:id` - Обновить новость
- `DELETE /api/news/:id` - Удалить новость

### Уведомления
- `GET /api/notifications` - Уведомления пользователя
- `GET /api/notifications/unread` - Непрочитанные уведомления
- `PUT /api/notifications/:id/read` - Отметить как прочитано

### Администрирование
- `POST /api/admin/modules` - Создать модуль
- `GET /api/admin/modules/group/:groupId` - Модули группы
- `PUT /api/admin/modules/:id/activate` - Активировать модуль
- `PUT /api/admin/modules/:id/complete` - Завершить модуль
- `GET /api/admin/stats` - Статистика колледжа
- `POST /api/admin/users` - Создать пользователя
- `GET /api/admin/admins` - Все администраторы

## Модели данных

### User
```javascript
{
  firstName: String,
  lastName: String,
  email: String,
  phone: String,
  password: String,
  role: String, // admin, president, rector, dean, teacher, student, group_leader
  avatar: String,
  isActive: Boolean
}
```

### Student
```javascript
{
  userId: ObjectId,
  studentId: String,
  group: ObjectId,
  academicStatus: String,
  enrollmentDate: Date,
  averageGrade: Number,
  totalAbsences: Number,
  totalLateArrivals: Number,
  successRate: Number
}
```

### Grade
```javascript
{
  student: ObjectId,
  subject: String,
  teacher: ObjectId,
  type: String, // module, session, final
  score: Number,
  gradeCategory: String,
  module: Number,
  semester: Number,
  notes: String
}
```

## Функциональность

### Для студентов:
- ✅ Просмотр расписания занятий с кабинетами и временем
- ✅ Отслеживание посещаемости и опозданий
- ✅ Просмотр оценок по модулям и сессиям
- ✅ Анализ своей успеваемости
- ✅ Просмотр новостей и объявлений колледжа
- ✅ Получение уведомлений

### Для преподавателей:
- ✅ Управление своими группами
- ✅ Отметить посещаемость
- ✅ Внести оценки за модули и сессии
- ✅ Просмотр аналитики группы
- ✅ Создание новостей и объявлений

### Для администрации:
- ✅ Управление пользователями и ролями
- ✅ Создание и управление группами
- ✅ Управление расписанием
- ✅ Создание и активация модулей
- ✅ Просмотр глобальной аналитики
- ✅ Управление новостями

## Основные особенности

1. **Ролевая система доступа** - разные уровни доступа для разных пользователей
2. **Адаптивный дизайн** - работает на всех устройствах
3. **Система уведомлений** - автоматические уведомления о важных событиях
4. **Аналитика** - подробная информация о посещаемости и успеваемости
5. **Безопасность** - JWT токены и хеширование паролей
6. **API документация** - полная документация всех endpoint'ов

## Технологический стек

### Backend
- Node.js
- Express.js
- MongoDB
- Mongoose
- JWT (JSON Web Tokens)
- bcryptjs

### Frontend
- React 18
- React Router
- Ant Design
- Axios
- Recharts
- Chart.js

## Развертывание

### Production сборка

```bash
# Frontend
cd frontend
npm run build

# Backend готов к развертыванию (используйте PM2 или Docker)
```

## Лицензия

MIT

## Автор

AL TAMIMI College Management Team

---

**Дедлайн:** 31 января 2026 года

Система готова к использованию и может быть расширена в зависимости от потребностей колледжа.
